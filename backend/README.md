# le gestionnaire de package est uv pour ce projet

`uv add` pour ajouter une dépendance au projet

`uv run python -m django startproject project .` donc uv run pour lancer une commande à l'intérieur de l'environnement virtuel